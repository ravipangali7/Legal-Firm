/**
 * Production Django origin for uploaded media and API when paths are root-relative (`/media/...`).
 * Used by `cmsMediaSrc` when `VITE_API_URL` is not set.
 */
export const BACKEND_BASE_URL = 'https://legalfirmserver.360winx.com';
