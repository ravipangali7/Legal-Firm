import DynamicFooter from '@/components/cms/DynamicFooter';

/** Marketing footer — same CMS-driven block as the homepage (`GET /api/site/homepage/` footer). */
const Footer = DynamicFooter;
export default Footer;
