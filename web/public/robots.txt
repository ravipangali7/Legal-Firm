# Served dynamically in production via /api/public/robots.txt (proxied as /robots.txt).
# Local static fallback — use absolute Sitemap when deploying without the API proxy.
User-agent: *
Allow: /

Sitemap: https://taxlexis.com/sitemap.xml
